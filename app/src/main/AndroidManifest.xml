<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.br.undcon">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Undconandroid"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.activity.SelectInventoryActivity"
            android:label="@string/title_activity_select_inventory"
            android:theme="@style/Theme.Undconandroid.NoActionBar"></activity>
        <activity
            android:name=".ui.activity.LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.MenuActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Undconandroid.NoActionBar" />
    </application>

</manifest>